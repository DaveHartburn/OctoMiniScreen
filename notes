The API key must be supplied in the custom HTTP header X-Api-Key, e.g.

GET /api/files HTTP/1.1
Host: example.com
X-Api-Key: abcdef...

If it is missing or included but invalid, OctoPrint will directly return a response with status 401

If not otherwise stated OctoPrint\u2019s API expects request bodies and issues response bodies as Content-Type: application/json.

GET /api/version
200 = OK

GET /api/connection  # connections status
http://docs.octoprint.org/en/master/api/connection.html
  # Can instruct a connect from here
  

ctoprint.org/en/master/api/job.html
  start, cancel, restart or pause the print job
  Get information about the current job, progress completion
  
# Printer operations
http://docs.octoprint.org/en/master/api/printer.html
  heating, moving etc
  Get current printer status
  /api/printer/command - send printer command, e.g. command: M106
  
http://docs.octoprint.org/en/master/api/datamodel.html


# GPIO control

  
https://github.com/vitormhenrique/OctoPrint-Enclosure/wiki/API-Control
